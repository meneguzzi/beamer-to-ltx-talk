% Beamer side of #26: a containsverbatim frame followed by an ORDINARY frame.
% convert_deck.py turns the first into frame* and stars its \end correctly on run 1;
% the bug was that run 2 stayed "inside" past that \end{frame*} and starred the
% ordinary frame's closer too. Both frames are load-bearing, and so is their order.
\documentclass{beamer}
\usepackage{listings}

\begin{document}
\begin{frame}[containsverbatim]{Verbatim}
\begin{lstlisting}
x = 1
\end{lstlisting}
\end{frame}

\begin{frame}{Ordinary frame after the verbatim one}
Body.
\end{frame}
\end{document}
