# before.tex is the Beamer original, so pdfTeX; the ltx-talk variants must be lualatex
# (SKILL.md Step 3).
ENGINES_before="pdflatex"
ENGINES_after="lualatex"
ENGINES_naive="lualatex"

# Two passes. In a fresh build directory one pass ships an extra "Temporary page! LaTeX was
# unable to guess the total number of pages correctly" page, which moves every page index.
PASSES=2
