# Beamer typesets the subtitle. This is the reference: the word must be here,
# otherwise the fixture is not testing anything.
. "$ASSERT_LIB"
must_contain "Quiz"
must_contain "Constraint Satisfaction"
