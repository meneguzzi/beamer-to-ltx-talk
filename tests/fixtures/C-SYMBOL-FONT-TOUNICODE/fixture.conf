# Both variants are ltx-talk and must be lualatex (SKILL.md Step 3). No before.tex: see the
# head comment in after.tex -- the Beamer original has the same corruption.
ENGINES_after="lualatex"
ENGINES_naive="lualatex"
