# The Beamer original is a pdfTeX build (it predates the conversion); both ltx-talk variants
# must be lualatex, which SKILL.md Step 3 requires anyway.
ENGINES_before="pdflatex"
ENGINES_after="lualatex"
ENGINES_naive="lualatex"
